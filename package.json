{
  "name": "ai-voice-agent",
  "private": true,
  "version": "1.0.0",
  "description": "Full-stack SaaS app for creating AI voice agents.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npm start --prefix backend"
  }
}
